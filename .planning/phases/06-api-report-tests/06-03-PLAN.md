---
phase: 06-api-report-tests
plan: 03
type: execute
depends_on: []
files_modified: [cypress/e2e/report-diagnoses.cy.ts]
---

<objective>
Test the client-side diagnosis report rendered on the report section, verifying that correct diagnosis sections appear based on the /dev route's pre-filled mock data.

Purpose: Ensure the report section accurately displays sleep metrics, identified issues, recommendations, and safety information based on the questionnaire responses processed by diagnosis-algorithms.ts.
Output: New test file `cypress/e2e/report-diagnoses.cy.ts` with passing tests.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/questionnaire/sections/ReportSection.tsx
@src/lib/diagnosis-algorithms.ts
@src/components/questionnaire/QuestionnaireForm.tsx (lines 16-159 for MOCK_DATA)

**What the mock data triggers (analysis of MOCK_DATA through ReportSection logic):**

The /dev route pre-fills MOCK_DATA which produces these diagnoses in the report:

PRESENT (should appear):
1. **Symptoms of Sleep Apnea Disorder** — snores=true + wakesWithDryMouth=true → hasOSA=true
2. **Sleep Hygiene Issues** — lastCaffeineTime='14:00' (hour 14 ≥ 14) → hasPoorHygiene=true
3. **Symptoms of Delayed Sleep Phase Disorder** — preference='late' → chronotype='delayed'
4. **Mild Respiratory Disturbance** — ABSENT (hasOSA=true suppresses this via `!hasOSA && ...`)
5. **Sleep Apnea Evaluation Recommended** — in "Next Steps" section, hasOSA=true triggers this
6. **Recommendations: For Your Insomnia Symptoms** — ABSENT (insomnia='none')
7. **Recommendations: Sleep Apnea Evaluation** — hasOSA triggers this
8. **Recommendations: For Delayed Sleep Phase** — chronotype='delayed' triggers this
9. **General Sleep Hygiene** — always shown

ABSENT (should NOT appear):
- Insomnia (SOL=30 not >30, WASO=30 not >40, earlyWakeup=15 not >20)
- EDS (fallAsleepDuring score=2, severity='none'; naps=2 days/week, <3)
- COMISA (no insomnia)
- RLS (troubleLyingStill=false)
- Nightmares disorder (nightmaresPerWeek=1, <3)
- Insufficient sleep (weeklyAvg=~7.2h, ≥7)
- Chronic fatigue (jointMusclePain=false)
- Severe tiredness safety warning (sleepinessSeverity=5, ≤8)
- Safety warnings section (no EDS, no parasomnia risk, no medication/alcohol risk)

**Sleep metrics expected (from MOCK_DATA):**
- Scheduled: bedtime 23:00, wake 07:00, SOL 30min, WASO 30min → TST = 7.0h, SE = 87.5%
- Unscheduled: bedtime 00:30, wake 09:00, SOL 20min, WASO 20min → TST = 7.8h, SE = 92.2%
- Weekly avg: (7.0×5 + 7.8×2)/7 ≈ 7.2h
- Chronotype label: "Probable Owl (Night Person)" (mid-sleep on free days is late + preference='late')

**Established patterns:**
- Use `cy.navigateToSection('report')` to load the report with pre-filled mock data
- Use `.should('contain.text', ...)` for text assertions
- Use `.should('not.exist')` or `.should('not.contain.text', ...)` for absence assertions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify diagnosis sections and identified issues</name>
  <files>cypress/e2e/report-diagnoses.cy.ts</files>
  <action>
Create `cypress/e2e/report-diagnoses.cy.ts` with tests verifying which diagnosis sections appear and which don't.

```
describe('Report Section - Diagnosis Verification', () => {
  beforeEach(() => {
    cy.navigateToSection('report')
  })

  describe('Report structure', () => {
    - displays "Your Sleep Health Report" title
    - displays "Your Sleep Metrics" card
    - displays "Identified Sleep Issues" card
    - displays "Recommendations" card
    - displays "Next Steps" card
    - displays "Resources" card
    - displays disclaimer alert
    - displays Print Report and Download PDF buttons
  })

  describe('Identified issues - present', () => {
    - displays "Symptoms of Sleep Apnea Disorder"
    - displays "Sleep Hygiene Issues"
    - displays "Symptoms of Delayed Sleep Phase Disorder"
  })

  describe('Identified issues - absent', () => {
    - does NOT display "Symptoms of Insomnia Disorder"
    - does NOT display "Excessive Daytime Sleepiness"
    - does NOT display "Symptoms of COMISA"
    - does NOT display "Symptoms of Restless Legs Syndrome"
    - does NOT display "Symptoms of Nightmare Disorder"
    - does NOT display "Symptoms of Insufficient Sleep Syndrome"
    - does NOT display "Symptoms of Chronic Fatigue"
    - does NOT display "No Major Sleep Issues Detected" (because issues ARE detected)
    - does NOT display "Urgent Safety Warning" (sleepinessSeverity ≤ 8)
  })

  describe('Next Steps section', () => {
    - displays "Sleep Apnea Evaluation Recommended"
    - does NOT display "Sleep Consultation Recommended" (no severe insomnia)
  })
})
```

For "absent" assertions, use:
`cy.contains('Symptoms of Insomnia Disorder').should('not.exist')`

Be careful with substring matches — use specific enough text to avoid false positives. For example, "Symptoms of Sleep Apnea Disorder" is unique enough. If needed, scope assertions within a specific card using `.find()` or `.within()`.
  </action>
  <verify>pnpm test:e2e -- --spec cypress/e2e/report-diagnoses.cy.ts</verify>
  <done>
- All present diagnosis tests pass (OSA, sleep hygiene, delayed sleep phase)
- All absent diagnosis tests pass (insomnia, EDS, COMISA, RLS, nightmares, insufficient sleep, chronic fatigue)
- Report structure tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify sleep metrics and recommendation text</name>
  <files>cypress/e2e/report-diagnoses.cy.ts</files>
  <action>
Add tests to `cypress/e2e/report-diagnoses.cy.ts` verifying metric displays and recommendation text:

```
  describe('Sleep metrics', () => {
    - displays "Work/School Days" section with sleep duration, efficiency, SOL, WASO
    - displays "Weekends/Free Days" section with sleep duration, efficiency, SOL, WASO
    - displays "Weekly Average Sleep" value
    - displays "Social Jet Lag" value
    - displays "Chronotype Assessment" with "Probable Owl (Night Person)"
  })

  describe('Recommendations', () => {
    - displays sleep apnea evaluation recommendation text ("we recommend evaluation for sleep apnea")
    - displays delayed sleep phase recommendation text ("light therapy, melatonin")
    - displays "General Sleep Hygiene" recommendation (always shown)
    - does NOT display insomnia recommendation text ("CBT-I")
  })

  describe('Safety warnings', () => {
    - "Important Safety Warnings" card does NOT appear (no qualifying conditions in mock data)
  })

  describe('Resources and disclaimer', () => {
    - displays "SomnaHealth Services" text in resources section
    - displays "Important Disclaimer" text
    - displays disclaimer text about informational purposes
  })
```

For metric value checks: use `cy.contains()` with partial text matches. The exact values depend on the MOCK_DATA calculations:
- Scheduled TST: "7.0 hours"
- Unscheduled TST: "7.8 hours" (approximately — verify the exact rendered value)
- Sleep efficiency values will be percentages
- SOL: "30 minutes" (scheduled), "20 minutes" (unscheduled)
- WASO: "30 minutes" (scheduled), "20 minutes" (unscheduled)

For "Important Safety Warnings" absence: the entire card should not render. Check with `cy.contains('Important Safety Warnings').should('not.exist')`.
  </action>
  <verify>pnpm test:e2e -- --spec cypress/e2e/report-diagnoses.cy.ts</verify>
  <done>
- Sleep metric values display correctly
- Chronotype assessment shows "Probable Owl"
- Correct recommendations appear/don't appear
- Safety warnings card absent
- Resources and disclaimer present
- Phase 6 complete
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test:e2e -- --spec cypress/e2e/report-diagnoses.cy.ts` — all tests pass
- [ ] No TypeScript errors
- [ ] Existing tests unaffected
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Report diagnoses verified against mock data analysis
- Metrics, recommendations, safety warnings, and structure all tested
- No errors or warnings introduced
- Phase 6 complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-api-report-tests/06-03-SUMMARY.md`
</output>
