---
phase: 05-client-feedback-regression
plan: 03
type: execute
depends_on: []
files_modified: [cypress/e2e/feedback-supplements.cy.ts]
---

<objective>
Supplement remaining feedback item gaps across items 1, 4, 5, 6, 8, 9, and 10. These are smaller gaps that need tightening rather than wholesale new coverage.

Purpose: The existing `round3-feedback.cy.ts` covers each of these items but misses specific edge cases: 0-minute is not verified as the FIRST option (item 4), hour picker isn't tested on unscheduled-sleep (item 5), work/school AM default isn't verified (item 8), and removal items lack structural integrity checks (items 6, 9). This plan closes those gaps.
Output: `cypress/e2e/feedback-supplements.cy.ts` — new test file with ~10 tests supplementing existing coverage.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@cypress/e2e/round3-feedback.cy.ts
@cypress/support/commands.ts
@src/components/questionnaire/sections/ScheduledSleepSection.tsx
@src/components/questionnaire/sections/UnscheduledSleepSection.tsx
@src/components/questionnaire/sections/DaytimeSection.tsx
@src/components/questionnaire/sections/ParasomniaSection.tsx
@src/components/questionnaire/sections/NightmaresSection.tsx
@src/components/questionnaire/sections/ChronotypeSection.tsx
@docs/correspondence/round-3-edits/CHANGES-IMPLEMENTED.md

**What exists and what's missing:**

| Item | Existing coverage | Gap this plan fills |
|------|------------------|---------------------|
| 1. Birth year range | Tests maxYear and tooRecent absence | Verify the OLDEST available year (lower bound) exists — ensures range isn't accidentally truncated |
| 4. 0-minute option | Tests it exists in both sections | Verify it's the FIRST option in dropdown (index 0) |
| 5. Hour picker | All 12 hours on scheduled-sleep | All 12 hours on UNSCHEDULED-sleep (covered in 05-01 plan) — skip here |
| 6. Narcolepsy removal | Text absence | Verify cataplexy/weakness questions STILL exist (structural integrity after removal) |
| 8. Work/school conditional | Hide/show tested | Verify work/school time field defaults to AM period |
| 9. Parasomnia diagnosis removal | Text absence | Verify night behavior checkboxes and bedwetting STILL exist (structural integrity) |
| 10. Nightmare factors removal | Contributing Factors absence | Verify nightmare frequency question is specifically the ONLY remaining follow-up |

**Constraining decisions:**
- Phase 01: Custom commands available for checkbox, select, radio interactions
- Phase 04: Conditional logic tests already cover most conditional visibility — don't duplicate

**What NOT to duplicate:**
- `round3-feedback.cy.ts` already tests all item text removals
- `sections-intro-daytime.cy.ts` already tests daytime section structure
- `sections-sleep-parasomnia.cy.ts` already tests parasomnia structure
- `conditional-nightmares-parasomnia.cy.ts` already tests nightmare follow-up conditionals
- Plan 05-01 covers unscheduled-sleep hour picker — skip here
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test 0-minute first position, birth year lower bound, and work/school AM default</name>
  <files>cypress/e2e/feedback-supplements.cy.ts</files>
  <action>
Create `cypress/e2e/feedback-supplements.cy.ts` with a top-level describe "Client Feedback: Supplemental Verification".

**0-minute is FIRST option (2 tests) — strengthens item 4:**

1. Scheduled-sleep: Navigate to `scheduled-sleep`. Open the time-to-fall-asleep dropdown by finding `cy.contains('how long does it take you to fall asleep').parent().find('button[role="combobox"]').click()`. Get the first `[role="option"]` element and assert its text is "0 minutes". Use `cy.get('[role="option"]').first().should('contain', '0 minutes')`.

2. Unscheduled-sleep: Navigate to `unscheduled-sleep`. Same test — open the fall-asleep dropdown, verify first option is "0 minutes".

**Birth year lower bound (1 test) — strengthens item 1:**

3. Navigate to `demographics`. Open the birth year dropdown by clicking the year combobox button (`cy.contains('button', '1990').click()`). Scroll or search for the oldest year. The dropdown should contain a reasonable lower bound (check that 1920 or similar old year exists). Use the search input `input[placeholder="Type a year..."]` to search for "1920" and verify it appears. Then search for "1919" (or a year clearly below range) and verify "No year found."

**Work/school time AM default (1 test) — strengthens item 8:**

4. Navigate to `chronotype`. Mock data has `preference: 'late'` so work/school time is visible. Find the work/school time field (`cy.contains('what time do you have to be at work/school?').closest('[data-slot="form-item"]')`). Within it, find the AM/PM combobox (3rd combobox, index 2) and verify it displays "AM". This confirms `defaultPeriod='AM'` is working for the chronotype time field.
  </action>
  <verify>pnpm test:e2e --spec cypress/e2e/feedback-supplements.cy.ts passes with 4 tests</verify>
  <done>4 tests pass — 0-minute confirmed as first option in both sections, birth year lower bound validated, work/school AM default confirmed</done>
</task>

<task type="auto">
  <name>Task 2: Verify structural integrity after removal items (items 6, 9, 10)</name>
  <files>cypress/e2e/feedback-supplements.cy.ts</files>
  <action>
Append to `cypress/e2e/feedback-supplements.cy.ts`:

**Daytime page — narcolepsy removed, other content intact (2 tests) — strengthens item 6:**

5. Navigate to `daytime`. Verify "narcolepsy or hypersomnia" text does NOT exist (quick re-assertion for context). Then verify that the cataplexy/weakness questions STILL exist: `cy.contains('experience sudden muscle weakness or cataplexy')` should be visible (or similar text). This ensures the narcolepsy removal didn't accidentally remove related EDS questions.

6. Verify "I take planned naps how many days per week?" still exists and "I take how many planned naps per week total?" does NOT exist — confirm item 3 removal didn't affect the remaining nap question. (Quick re-check for structural integrity.)

**Parasomnia page — diagnosis removed, behaviors intact (2 tests) — strengthens item 9:**

7. Navigate to `parasomnia`. Verify "I have been diagnosed with a parasomnia" does NOT exist. Then verify the night behavior checkboxes still exist: check for text like "Walk", "Talk", "Eat" or the specific labels used in the night behaviors checklist. This ensures the diagnosis removal didn't break the behavior section.

8. Verify the bedwetting checkbox still exists: `cy.contains('bedwetting').should('exist')`.

**Nightmares page — factors removed, frequency intact (2 tests) — strengthens item 10:**

9. Navigate to `nightmares`. Verify "Contributing Factors" does NOT exist. Then verify that ONLY the frequency question remains as the nightmare follow-up: `cy.contains('How many nights a week do you have nightmares?').should('be.visible')`. Verify there are no other follow-up fields between the `hasNightmares` section and the trauma checkbox: the "associated with a traumatic event" text should exist (confirming trauma question wasn't accidentally removed with contributing factors).

10. Verify the nightmare frequency field and trauma checkbox are the ONLY two follow-up items — no orphaned contributing factor checkboxes remain. Check that "history of traumatic brain injury", "medications that may cause nightmares", "behavioral health", and "sleep aversion" do NOT exist (mirrors existing test but grouped for structural context).

**Important:** Keep these tests lean. They're structural integrity checks, not deep functional tests. Each test should assert 1-2 things clearly.
  </action>
  <verify>pnpm test:e2e --spec cypress/e2e/feedback-supplements.cy.ts passes with all ~10 tests</verify>
  <done>All 10 tests pass — structural integrity verified for all removal items, 0-minute first-position confirmed, birth year bounds validated, work/school AM default confirmed. Phase 5 complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test:e2e --spec cypress/e2e/feedback-supplements.cy.ts` — all tests pass
- [ ] `pnpm test:e2e --spec cypress/e2e/round3-feedback.cy.ts` — existing tests still pass
- [ ] No duplicate assertions with existing test files
</verification>

<success_criteria>

- All tasks completed
- All tests in `feedback-supplements.cy.ts` pass
- 0-minute verified as first dropdown option in both sleep sections
- Birth year lower bound verified
- Work/school time AM default verified
- Structural integrity confirmed for all removal items (6, 9, 10)
- No regressions in existing test files
- Phase 5 complete — all 12 feedback items now have strengthened/supplemented coverage
</success_criteria>

<output>
After completion, create `.planning/phases/05-client-feedback-regression/05-03-SUMMARY.md`
</output>
