---
phase: 05-client-feedback-regression
plan: 01
type: execute
depends_on: []
files_modified: [cypress/e2e/feedback-ampm.cy.ts]
---

<objective>
Strengthen AM/PM default and warning test coverage (feedback items 5 and 7) — the weakest area in the existing `round3-feedback.cy.ts`.

Purpose: The existing tests for item 7 only assert that a `<span>` exists inside the AM/PM combobox — they never verify the actual default value is PM for bedtime and AM for wake time. Warnings are only tested for bedtime-AM on scheduled-sleep; wake-time-PM warnings and all unscheduled-sleep warnings are untested. Item 5 (hour picker) only covers scheduled-sleep.
Output: `cypress/e2e/feedback-ampm.cy.ts` — new test file with ~15 tests covering AM/PM defaults, all 4 warning scenarios across both sleep sections, and unscheduled-sleep hour picker.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@cypress/e2e/round3-feedback.cy.ts
@cypress/support/commands.ts
@cypress/support/test-data.ts
@src/components/questionnaire/sections/ScheduledSleepSection.tsx
@src/components/questionnaire/sections/UnscheduledSleepSection.tsx
@src/components/questionnaire/form-fields/TimeField.tsx

**Established patterns:**
- `cy.navigateToSection(section)` visits `/dev?section={section}` and waits for `[data-slot="card-title"]`
- `cy.selectOption(label, value)` opens shadcn Select by label, clicks option
- `cy.contains(label).closest('[data-slot="form-item"]').find('button[role="combobox"]')` to locate TimeField selects
- AM/PM warnings trigger via `isUnusualBedtime()` (hour 4-17 = AM daytime) and `isUnusualWakeTime()` (hour >= 14 or < 2 = PM evening)
- Mock data: scheduled lightsOutTime='23:00', wakeupTime='07:00'; unscheduled lightsOutTime='00:30', wakeupTime='09:00' — none trigger warnings by default

**Constraining decisions:**
- Phase 01: `data-slot="card-title"` added to CardTitle for test infrastructure selectors
- Phase 01: Use `closest('[data-slot="form-item"]').find('button[role="checkbox"]')` for state assertions
- Phase 01: Custom commands `selectOption`, `clickRadio`, `checkCheckbox`, `uncheckCheckbox` available

**What NOT to duplicate:**
- `round3-feedback.cy.ts` already tests: bedtime-AM warning appearance on scheduled-sleep (item 12/7), basic PM/AM span existence, all 12 hours on scheduled-sleep hour picker
- `sections-sleep-parasomnia.cy.ts` already tests: no AM/PM warning for 23:00 bedtime (negative case)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test AM/PM default values on both sleep sections and work/school time</name>
  <files>cypress/e2e/feedback-ampm.cy.ts</files>
  <action>
Create `cypress/e2e/feedback-ampm.cy.ts` with a top-level describe "Client Feedback: AM/PM Defaults & Warnings".

**Scheduled-sleep defaults (3 tests):**
- Navigate to `scheduled-sleep`. The mock data pre-fills lightsOutTime='23:00' (11 PM) and wakeupTime='07:00' (7 AM).
- Verify bedtime AM/PM combobox shows "PM" — use `cy.contains('turn out the lights').closest('[data-slot="form-item"]').find('button[role="combobox"]').last()` and assert its text includes "PM".
- Verify wake time AM/PM combobox shows "AM" — use `cy.contains('What time do you wake up?').closest('[data-slot="form-item"]').find('button[role="combobox"]').last()` and assert its text includes "AM".
- Verify get-out-of-bed time AM/PM combobox shows "AM".

**Unscheduled-sleep defaults (3 tests):**
- Navigate to `unscheduled-sleep`. Mock data: lightsOutTime='00:30' (12:30 AM), wakeupTime='09:00' (9 AM).
- Same 3 checks as scheduled-sleep (bedtime shows "AM" because 00:30 is 12:30 AM, wake shows "AM", get-out-of-bed shows "AM").
- Note: The unscheduled bedtime mock is 00:30 which converts to 12:30 AM — verify the combobox displays "AM" for this case.

**Work/school time AM default (1 test):**
- Navigate to `chronotype`. Mock data has `preference: 'late'` so work/school time field is visible.
- Verify the work/school time AM/PM combobox shows "AM" (defaultPeriod='AM' in ChronotypeSection.tsx).
  </action>
  <verify>pnpm test:e2e --spec cypress/e2e/feedback-ampm.cy.ts passes with 7 tests</verify>
  <done>7 tests pass verifying actual AM/PM text values in comboboxes across scheduled-sleep, unscheduled-sleep, and chronotype sections</done>
</task>

<task type="auto">
  <name>Task 2: Test all 4 warning scenarios and unscheduled-sleep hour picker</name>
  <files>cypress/e2e/feedback-ampm.cy.ts</files>
  <action>
Append to `cypress/e2e/feedback-ampm.cy.ts`:

**Scheduled-sleep warnings (2 tests — supplement existing):**
- Wake-time PM warning: Navigate to scheduled-sleep. Change wake time's AM/PM to "PM" by clicking the 3rd combobox in the wake time form-item, selecting "PM". Assert "Your wake time appears to be set during evening/nighttime hours" is visible. (The bedtime-AM warning test already exists in round3-feedback.cy.ts — do NOT duplicate it.)
- Warning clears: After triggering wake-time PM warning, change AM/PM back to "AM". Assert the warning disappears.

**Unscheduled-sleep warnings (4 tests — all new):**
- Bedtime AM warning: Navigate to unscheduled-sleep. The mock lightsOutTime is '00:30' which is midnight (hour 0) — NOT in the unusual range (4-17). We need to set bedtime to an AM daytime hour. Change the hour combobox to "10" and ensure AM/PM is "AM". Assert "Your bedtime appears to be set during daytime hours" is visible.
- Bedtime warning clears: Change AM/PM back to "PM". Assert warning disappears.
- Wake-time PM warning: Change wake time AM/PM to "PM". Assert "Your wake time appears to be set during evening/nighttime hours" is visible.
- Wake-time warning clears: Change back to "AM". Assert warning disappears.

**Unscheduled-sleep hour picker (1 test — supplements item 5):**
- Navigate to unscheduled-sleep. Click the first hour combobox. Verify all 12 hour options (1-12) are present in `[role="option"]` elements. (The existing test in round3-feedback.cy.ts only covers scheduled-sleep.)

**Important implementation notes:**
- TimeField renders 3 comboboxes within each `[data-slot="form-item"]`: hour (index 0), minute (index 1), AM/PM (index 2). Use `.eq(2)` to target the AM/PM combobox.
- To select an option: click the combobox, then `cy.get('[role="option"]').contains('PM').click()`.
- Close any open popovers with `cy.get('body').click(0, 0)` or `{esc}` if needed between interactions.
  </action>
  <verify>pnpm test:e2e --spec cypress/e2e/feedback-ampm.cy.ts passes with all ~14 tests</verify>
  <done>All 14 tests pass — AM/PM defaults verified on 3 sections, all 4 warning scenarios (bedtime-AM + wake-PM) tested on both sleep sections with clear/re-trigger behavior, unscheduled-sleep hour picker verified</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test:e2e --spec cypress/e2e/feedback-ampm.cy.ts` — all tests pass
- [ ] `pnpm test:e2e --spec cypress/e2e/round3-feedback.cy.ts` — existing tests still pass (no regressions)
- [ ] No duplicate tests between feedback-ampm.cy.ts and round3-feedback.cy.ts
</verification>

<success_criteria>

- All tests in `feedback-ampm.cy.ts` pass
- AM/PM defaults verified with actual text values (not just span existence)
- All 4 warning scenarios tested across both sleep sections
- Unscheduled-sleep hour picker verified
- No regressions in existing test files
</success_criteria>

<output>
After completion, create `.planning/phases/05-client-feedback-regression/05-01-SUMMARY.md`
</output>
