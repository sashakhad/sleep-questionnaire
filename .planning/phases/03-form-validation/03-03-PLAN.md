---
phase: 03-form-validation
plan: 03
type: execute
depends_on: []
files_modified: [cypress/e2e/validation-time-fields.cy.ts]
---

<objective>
Test that time fields across sections render correctly with hour/minute/AM-PM select dropdowns, produce valid time values, and respect default AM/PM periods.

Purpose: Verify time picker UI works correctly — selections produce valid HH:MM format strings and default periods are applied as expected.
Output: `cypress/e2e/validation-time-fields.cy.ts` with passing tests for time field behavior.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@cypress/support/commands.ts
@cypress/support/assertions.ts
@cypress/support/test-data.ts
@src/components/questionnaire/form-fields/TimeField.tsx
@src/components/questionnaire/sections/ScheduledSleepSection.tsx
@src/components/questionnaire/sections/ChronotypeSection.tsx
@src/validations/questionnaire.ts

**Established patterns:**
- `cy.navigateToSection(section)` visits `/dev?section={section}` with pre-filled mock data
- Time fields use three `Select` components (hour, minute, AM/PM) from shadcn/ui
- `SelectTrigger` has `role="combobox"`, `SelectItem` has `role="option"`

**Key context:**
- TimeField component renders 3 select dropdowns: hour (12,1-11), minute (00,15,30,45), period (AM,PM)
- Each TimeField has a `defaultPeriod` prop (defaults to 'PM')
- Time fields convert between 12-hour display and 24-hour storage (HH:MM format)
- Time fields appear in: scheduled-sleep (lightsOutTime, wakeupTime, getOutOfBedTime), unscheduled-sleep (same), chronotype (workSchoolTime), sleep-hygiene (stimulantTime), lifestyle (lastCaffeineTime, exerciseEndTime)
- Zod validation: `timeString` accepts empty string or `HH:MM` format — UI enforces this via select dropdowns
- Pre-filled mock data populates time fields with valid values on dev route

**Sections with time fields:**
- scheduled-sleep: lightsOutTime, wakeupTime, getOutOfBedTime (3 fields)
- unscheduled-sleep: lightsOutTime, wakeupTime, getOutOfBedTime (3 fields)
- chronotype: workSchoolTime (1 field)
- sleep-hygiene: stimulantTime (1 field)
- lifestyle: lastCaffeineTime, exerciseEndTime (2 fields)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create time field validation test file</name>
  <files>cypress/e2e/validation-time-fields.cy.ts</files>
  <action>
Create `cypress/e2e/validation-time-fields.cy.ts` with describe blocks for time field behavior across sections.

Tests to write:

**Scheduled Sleep time fields:**
1. **Time field renders three select dropdowns** — Navigate to scheduled-sleep via dev route, find the "Lights Out Time" label's parent FormItem, verify it contains 3 elements with `role="combobox"` (hour, minute, period selects).

2. **Hour select shows all 12 options** — Click the hour select trigger for "Lights Out Time", verify 12 options exist (12, 1, 2, ... 11).

3. **Minute select shows 15-minute increments** — Click the minute select trigger, verify 4 options (00, 15, 30, 45).

4. **AM/PM select shows both periods** — Click the period select trigger, verify 2 options (AM, PM).

5. **Selecting time values updates the field** — Select hour "10", minute "30", period "PM" for a time field, verify the selects display the chosen values.

**Chronotype workSchoolTime:**
6. **Work/school time field renders on chronotype section** — Navigate to chronotype via dev route, verify the work/school start time field exists with 3 select dropdowns.

**Cross-section time field presence:**
7. **Unscheduled sleep has time fields** — Navigate to unscheduled-sleep, verify lightsOutTime, wakeupTime, getOutOfBedTime labels exist with select dropdowns.

8. **Lifestyle section has time fields** — Navigate to lifestyle, verify lastCaffeineTime and exerciseEndTime labels exist with select dropdowns.

Use `cy.navigateToSection()` for each section. Locate time fields by their `FormLabel` text, then find the parent `FormItem` container and assert on the select components within it.

For interacting with shadcn Select: click the `button[role="combobox"]` trigger, then select from `[role="option"]` items in the dropdown.

Do NOT test invalid time formats — the UI enforces valid times through select dropdowns, making invalid input impossible through the UI.
  </action>
  <verify>pnpm test:e2e --spec cypress/e2e/validation-time-fields.cy.ts</verify>
  <done>All time field tests pass. Select dropdowns render correctly, options are correct, and interactions work.</done>
</task>

<task type="auto">
  <name>Task 2: Verify tests pass and finalize phase</name>
  <files>cypress/e2e/validation-time-fields.cy.ts</files>
  <action>
Run the full Cypress test suite to confirm the new validation-time-fields tests don't conflict with existing tests. If any test fails, diagnose and fix the issue in the new test file only — do not modify existing test files.

This is the final plan in Phase 3. After all tests pass, Phase 3 (Form Validation) is complete.
  </action>
  <verify>pnpm test:e2e</verify>
  <done>All Cypress tests pass including all three Phase 3 validation test files and all existing tests. Phase 3 complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test:e2e --spec cypress/e2e/validation-time-fields.cy.ts` passes all tests
- [ ] `pnpm test:e2e` passes full suite without regressions
- [ ] No modifications to existing test files
</verification>

<success_criteria>

- `cypress/e2e/validation-time-fields.cy.ts` exists with 8+ tests
- All tests pass
- Time field select dropdowns verified (hour, minute, AM/PM)
- Correct options verified (12 hours, 4 minute increments, 2 periods)
- Time field interactions work (select → display updates)
- Multiple sections with time fields verified
- Phase 3 complete — all 3 plans delivered
</success_criteria>

<output>
After completion, create `.planning/phases/03-form-validation/03-03-SUMMARY.md`
</output>
