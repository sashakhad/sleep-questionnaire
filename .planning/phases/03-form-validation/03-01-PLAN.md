---
phase: 03-form-validation
plan: 01
type: execute
depends_on: []
files_modified: [cypress/e2e/validation-intro.cy.ts]
---

<objective>
Test that the intro section's disclaimer checkbox gates the Continue button — unchecked disables, checked enables.

Purpose: Verify the first validation gate in the questionnaire flow works correctly, preventing users from proceeding without accepting the disclaimer.
Output: `cypress/e2e/validation-intro.cy.ts` with passing tests for intro validation behavior.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@cypress/support/commands.ts
@cypress/support/assertions.ts
@cypress/support/test-data.ts
@src/components/questionnaire/sections/IntroSection.tsx
@src/validations/questionnaire.ts

**Established patterns:**
- `cy.navigateToSection(section)` visits `/dev?section={section}` with pre-filled mock data
- `assertContinueDisabled()` / `assertContinueEnabled()` helpers from `cypress/support/assertions.ts`
- CheckboxField DOM: use `closest('[data-slot="form-item"]').find('button[role="checkbox"]')` for state assertions

**Constraining decisions:**
- Phase 01: `data-slot="card-title"` added to CardTitle component — used by `assertSectionVisible`
- Phase 02: CheckboxField DOM structure requires specific traversal pattern for checkbox state

**Key context:**
- The main route (`/`) renders `QuestionnaireForm` without `prefill={true}`, so `acceptedDisclaimer` starts as `false` and Continue is disabled
- The `/dev` route pre-fills mock data with `acceptedDisclaimer: true`, so Continue starts enabled there
- `isContinueDisabled` in QuestionnaireForm checks `!acceptedDisclaimer` when on intro section
- Validation schema: `introSchema` requires `acceptedDisclaimer` to be `true` via `.refine(val => val === true)`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create intro validation test file</name>
  <files>cypress/e2e/validation-intro.cy.ts</files>
  <action>
Create `cypress/e2e/validation-intro.cy.ts` with a describe block for "Intro Section Validation".

Tests to write:

1. **Continue is disabled when disclaimer is unchecked** — Visit `/` (no prefill), wait for form, verify Continue button is disabled using `assertContinueDisabled()`.

2. **Continue becomes enabled after checking disclaimer** — Visit `/`, find the disclaimer checkbox via `button[role="checkbox"]` inside the disclaimer FormItem (contains text "I have read and understand"), click it, verify Continue becomes enabled via `assertContinueEnabled()`.

3. **Continue becomes disabled again after unchecking disclaimer** — Continue from above: click the checkbox again to uncheck, verify Continue is disabled again.

4. **Disclaimer starts checked on dev route (pre-filled)** — Visit `/dev?section=intro` via `cy.navigateToSection('intro')`, verify the checkbox has `data-state="checked"` and Continue is enabled.

Use the main route (`/`) for tests 1-3 since it starts with an empty form (disclaimer unchecked). Use the dev route for test 4 to verify the mock data path.

Do NOT use `cy.wait()` for timing — rely on Cypress's built-in retry/timeout via `.should()` assertions.
  </action>
  <verify>pnpm test:e2e --spec cypress/e2e/validation-intro.cy.ts</verify>
  <done>All intro validation tests pass. Continue button correctly reflects disclaimer checkbox state.</done>
</task>

<task type="auto">
  <name>Task 2: Verify tests pass alongside existing suite</name>
  <files>cypress/e2e/validation-intro.cy.ts</files>
  <action>
Run the full Cypress test suite to confirm the new validation-intro tests don't conflict with existing tests. If any test fails, diagnose and fix the issue in the new test file only — do not modify existing test files.
  </action>
  <verify>pnpm test:e2e</verify>
  <done>All Cypress tests pass including new intro validation tests and all existing tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test:e2e --spec cypress/e2e/validation-intro.cy.ts` passes all tests
- [ ] `pnpm test:e2e` passes full suite without regressions
- [ ] No modifications to existing test files
</verification>

<success_criteria>

- `cypress/e2e/validation-intro.cy.ts` exists with 4+ tests
- All tests pass
- Continue button correctly toggles disabled/enabled based on disclaimer state
- Both main route (empty form) and dev route (pre-filled) paths verified
</success_criteria>

<output>
After completion, create `.planning/phases/03-form-validation/03-01-SUMMARY.md`
</output>
