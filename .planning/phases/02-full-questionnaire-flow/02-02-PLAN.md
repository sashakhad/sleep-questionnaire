---
phase: 02-full-questionnaire-flow
plan: 02
type: execute
depends_on: []
files_modified: [cypress/e2e/sections-sleep-parasomnia.cy.ts]
---

<objective>
Create Cypress E2E tests verifying that sections scheduled-sleep, unscheduled-sleep, breathing-disorders, restless-legs, and parasomnia render correctly with pre-filled mock data via the /dev route.

Purpose: Cover the middle batch of questionnaire sections (5-9), ensuring each section's form fields, labels, conditional elements, and alerts render as expected given the mock data state.
Output: `cypress/e2e/sections-sleep-parasomnia.cy.ts` with tests for 5 questionnaire sections.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-test-infrastructure-navigation/01-01-SUMMARY.md
@.planning/phases/01-test-infrastructure-navigation/01-02-SUMMARY.md
@cypress/support/test-data.ts
@cypress/support/commands.ts
@cypress/support/assertions.ts
@src/components/questionnaire/sections/ScheduledSleepSection.tsx
@src/components/questionnaire/sections/UnscheduledSleepSection.tsx
@src/components/questionnaire/sections/BreathingDisordersSection.tsx
@src/components/questionnaire/sections/RestlessLegsSection.tsx
@src/components/questionnaire/sections/ParasomniaSection.tsx
@src/components/questionnaire/QuestionnaireForm.tsx (lines 16-159 for MOCK_DATA)

**Tech stack available:** Cypress 14.x, custom commands (navigateToSection, selectOption, clickRadio, checkCheckbox, uncheckCheckbox)
**Established patterns:** `cy.navigateToSection(slug)` for section navigation, `assertSectionVisible(title)` for card title checks, helpers from test-data.ts and assertions.ts

**Mock data relevant to these sections (from MOCK_DATA):**
- scheduledSleep: `{ lightsOutTime: '23:00', lightsOutVaries: false, preBedActivity: [], minutesToFallAsleep: '30', nightWakeups: 2, minutesAwakeAtNight: '30', wakeupTime: '07:00', getOutOfBedTime: '07:15', earlyWakeupDays: 1, usesAlarm: true }`
- unscheduledSleep: `{ lightsOutTime: '00:30', minutesToFallAsleep: '20', nightWakeups: 1, minutesAwakeAtNight: '20', wakeupTime: '09:00', getOutOfBedTime: '09:30', usesAlarm: false }`
- breathingDisorders: `{ snores: true, stopsBreathing: false, mouthBreathes: true, wakesWithDryMouth: true }`
- restlessLegs: `{ troubleLyingStill: false, urgeToMoveLegs: false, movementRelieves: false, daytimeDiscomfort: false, legCramps: false }`
- parasomnia: `{ nightBehaviors: [], remembersEvents: false, actsOutDreams: false, hasInjuredOrLeftHome: false, bedwetting: false }`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test scheduled-sleep and unscheduled-sleep sections render correctly</name>
  <files>cypress/e2e/sections-sleep-parasomnia.cy.ts</files>
  <action>
Create `cypress/e2e/sections-sleep-parasomnia.cy.ts`. Import `SECTION_TITLES` from `../support/test-data` and `assertSectionVisible` from `../support/assertions`.

**describe('Scheduled Sleep section'):**
- beforeEach: `cy.navigateToSection('scheduled-sleep')`
- Test: "should display the section title" — `assertSectionVisible(SECTION_TITLES['scheduled-sleep'])`
- Test: "should display lights out time field" — assert label "What time do you turn out the lights and try to fall asleep?" exists
- Test: "should display lights out varies radio" — assert "Does your lights out time vary more than 2 hours?" label exists
- Test: "should display time to fall asleep selector" — assert "After you turn out the lights, about how long does it take you to fall asleep?" label exists
- Test: "should display night wakeups field" — assert "About how many times do you wake up during the night" label exists
- Test: "should not display wakeup reasons with mock data" — mock data has `nightWakeups: 2` which is NOT > 2, so the "What wakes you?" section should NOT be visible
- Test: "should display minutes awake at night selector" — assert "About how many minutes total are you awake during the night?" label exists
- Test: "should display wake up and get out of bed time fields" — assert "What time do you wake up?" and "What time do you get out of bed?" labels exist
- Test: "should display early wakeup days and alarm clock fields" — assert "How many days a week do you wake up earlier than planned?" and "I use an alarm clock" labels exist
- Test: "should not show AM/PM warning for valid bedtime" — mock data lightsOutTime is 23:00 (11 PM, valid nighttime), so no "Your bedtime appears to be set during daytime hours" warning should display

**describe('Unscheduled Sleep section'):**
- beforeEach: `cy.navigateToSection('unscheduled-sleep')`
- Test: "should display the section title" — `assertSectionVisible(SECTION_TITLES['unscheduled-sleep'])`
- Test: "should display the natural sleep patterns info box" — assert text "This section helps us understand your natural sleep patterns" exists
- Test: "should display all expected form fields" — assert labels for lights out time, time to fall asleep, night wakeups, minutes awake at night, wake up time, get out of bed time, and alarm clock checkbox all exist
- Test: "should not display wakeup reasons with mock data" — mock data has `nightWakeups: 1` which is NOT > 2, so "What wakes you?" should NOT be visible

Do NOT use `cy.wait()`. Rely on Cypress retry/timeout behavior.
  </action>
  <verify>Run `pnpm test:e2e --spec cypress/e2e/sections-sleep-parasomnia.cy.ts` — all scheduled-sleep and unscheduled-sleep tests pass</verify>
  <done>Scheduled Sleep: 10 passing tests verifying all form fields, conditional wakeup reasons hidden, and no false AM/PM warning. Unscheduled Sleep: 4 passing tests verifying form fields and conditional behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Test breathing-disorders, restless-legs, and parasomnia sections render correctly</name>
  <files>cypress/e2e/sections-sleep-parasomnia.cy.ts</files>
  <action>
Append to the same test file created in Task 1.

**describe('Breathing Disorders section'):**
- beforeEach: `cy.navigateToSection('breathing-disorders')`
- Test: "should display the section title" — `assertSectionVisible(SECTION_TITLES['breathing-disorders'])`
- Test: "should display all breathing symptom checkboxes" — assert labels "I have been told that I snore", "I have been told that I stop breathing, snort or gasp for air during sleep", and "I mouth breathe" exist
- Test: "should show snoring as checked with mock data" — mock data has `snores: true`, verify the snoring checkbox is checked (`data-state="checked"` on the closest checkbox button)
- Test: "should show dry mouth checkbox when mouth breathing is checked" — mock data has `mouthBreathes: true`, so "I frequently wake up with a dry mouth" should be visible and checked (mock has `wakesWithDryMouth: true`)
- Test: "should display breathing disorder warning when snoring is checked" — mock data has `snores: true`, so the warning alert about treatment options should be visible. Assert text containing "provide important information in the report" exists

**describe('Restless Legs section'):**
- beforeEach: `cy.navigateToSection('restless-legs')`
- Test: "should display the section title" — `assertSectionVisible(SECTION_TITLES['restless-legs'])`
- Test: "should display RLS info alert" — assert text about "Restless legs syndrome is a relatively common disorder" exists
- Test: "should display all RLS symptom checkboxes" — assert labels for "trouble lying still", "urge to move my legs", "Movement relieves", "leg discomfort during the day", "leg cramps at night" exist
- Test: "should not show RLS warning with mock data" — mock data has all RLS symptoms false, so the warning "Your answers suggest that you may have restless legs syndrome" should NOT exist
- Test: "should not show leg cramp frequency field" — mock data has `legCramps: false`, so "How many nights per week" should NOT be visible

**describe('Parasomnia section'):**
- beforeEach: `cy.navigateToSection('parasomnia')`
- Test: "should display the section title" — `assertSectionVisible(SECTION_TITLES['parasomnia'])`
- Test: "should display parasomnia info alert" — assert text about "unusual behaviors during sleep" exists
- Test: "should display night behavior checkboxes" — assert labels "Walk", "Eating", "Appear confused", "Are very upset and cannot be calmed" exist
- Test: "should not show follow-up questions with mock data" — mock data has `nightBehaviors: []`, so "I have a clear memory of these events", "I act out my dreams", and "I have injured myself" should NOT be visible
- Test: "should display bedwetting checkbox" — assert "I wet the bed more than 1 night per month" label exists
  </action>
  <verify>Run `pnpm test:e2e --spec cypress/e2e/sections-sleep-parasomnia.cy.ts` — all tests pass including breathing-disorders, restless-legs, and parasomnia</verify>
  <done>Breathing Disorders: 5 tests verifying checkboxes, conditional dry mouth, and warning alert. Restless Legs: 5 tests verifying all symptom checkboxes, hidden warning, and hidden frequency. Parasomnia: 5 tests verifying night behaviors, hidden follow-ups, and bedwetting checkbox.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test:e2e --spec cypress/e2e/sections-sleep-parasomnia.cy.ts` passes all tests
- [ ] No TypeScript compilation errors in the test file
- [ ] Existing test files not modified
- [ ] All 5 sections have at least 4 tests each
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `cypress/e2e/sections-sleep-parasomnia.cy.ts` exists with 5 describe blocks
- Each section has tests verifying form field rendering and conditional visibility with mock data
- No modifications to existing test files
</success_criteria>

<output>
After completion, create `.planning/phases/02-full-questionnaire-flow/02-02-SUMMARY.md`
</output>
