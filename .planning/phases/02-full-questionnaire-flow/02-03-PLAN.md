---
phase: 02-full-questionnaire-flow
plan: 03
type: execute
depends_on: []
files_modified: [cypress/e2e/sections-nightmares-report.cy.ts]
---

<objective>
Create Cypress E2E tests verifying that sections nightmares, chronotype, sleep-hygiene, bedroom, lifestyle, mental-health, and report render correctly with pre-filled mock data via the /dev route.

Purpose: Complete the full questionnaire flow coverage by testing the final 7 sections (10-16), ensuring all form fields, conditional UI, and alerts render as expected with mock data.
Output: `cypress/e2e/sections-nightmares-report.cy.ts` with tests for the final 7 questionnaire sections.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-test-infrastructure-navigation/01-01-SUMMARY.md
@.planning/phases/01-test-infrastructure-navigation/01-02-SUMMARY.md
@cypress/support/test-data.ts
@cypress/support/commands.ts
@cypress/support/assertions.ts
@src/components/questionnaire/sections/NightmaresSection.tsx
@src/components/questionnaire/sections/ChronotypeSection.tsx
@src/components/questionnaire/sections/SleepHygieneSection.tsx
@src/components/questionnaire/sections/BedroomSection.tsx
@src/components/questionnaire/sections/LifestyleSection.tsx
@src/components/questionnaire/sections/MentalHealthSection.tsx
@src/components/questionnaire/sections/ReportSection.tsx
@src/components/questionnaire/QuestionnaireForm.tsx (lines 16-159 for MOCK_DATA)

**Tech stack available:** Cypress 14.x, custom commands (navigateToSection, selectOption, clickRadio, checkCheckbox, uncheckCheckbox)
**Established patterns:** `cy.navigateToSection(slug)` for section navigation, `assertSectionVisible(title)` for card title checks, helpers from test-data.ts and assertions.ts

**Mock data relevant to these sections (from MOCK_DATA):**
- nightmares: `{ remembersDreams: true, hasBadDreams: false, hasNightmares: true, nightmaresPerWeek: 1, associatedWithTrauma: false }`
- chronotype: `{ preference: 'late', preferenceStrength: 'moderate', shiftWork: false, pastShiftWorkYears: 0, frequentTimeZoneTravel: false, workSchoolTime: '09:00' }`
- sleepHygiene: `{ supplements: ['melatonin'], prescriptionMeds: [], stimulants: '', smokesNicotine: false }`
- bedroom: `{ relaxing: 7, comfortable: 8, dark: 6, quiet: 7 }`
- lifestyle: `{ caffeinePerDay: 2, lastCaffeineTime: '14:00', alcoholPerWeek: 3, exerciseDaysPerWeek: 3, exerciseDuration: 45, exerciseEndTime: '18:00' }`
- mentalHealth: `{ worriesAffectSleep: true, anxietyInBed: true, timeInBedTrying: true, cancelsAfterPoorSleep: '1-2week', diagnosedMedicalConditions: [], diagnosedMentalHealthConditions: ['anxiety'], currentlyReceivingTreatment: false }`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test nightmares, chronotype, and sleep-hygiene sections render correctly</name>
  <files>cypress/e2e/sections-nightmares-report.cy.ts</files>
  <action>
Create `cypress/e2e/sections-nightmares-report.cy.ts`. Import `SECTION_TITLES` from `../support/test-data` and `assertSectionVisible` from `../support/assertions`.

**describe('Nightmares section'):**
- beforeEach: `cy.navigateToSection('nightmares')`
- Test: "should display the section title" — `assertSectionVisible(SECTION_TITLES['nightmares'])`
- Test: "should display dream recall checkbox as checked" — mock data has `remembersDreams: true`, verify the checkbox for "I remember my dreams at least a few nights a week" is checked
- Test: "should show dream recall info alert when remembersDreams is checked" — assert text "Remembering your dreams can be a function of many factors" exists (shown conditionally when remembersDreams is true)
- Test: "should display dreams vs nightmares definitions" — assert "Understanding Dreams vs Nightmares" heading and definitions for "Bad dreams" and "Nightmares" exist
- Test: "should display nightmare frequency and trauma fields" — mock data has `hasNightmares: true`, so "How many nights a week do you have nightmares?" and "My nightmares are associated with exposure to trauma" should be visible
- Test: "should show less-frequent nightmares info for 1 per week" — mock data has `nightmaresPerWeek: 1` (>0 and <2), so the info alert about "Nightmares can disturb sleep" should be visible, but the "Possible Nightmare Disorder" warning (>=2) should NOT be visible

**describe('Chronotype section'):**
- beforeEach: `cy.navigateToSection('chronotype')`
- Test: "should display the section title" — `assertSectionVisible(SECTION_TITLES['chronotype'])`
- Test: "should display chronotype preference radio group" — assert "What is your natural sleep preference?" and options "morning person", "night owl", "flexible" exist
- Test: "should show preference strength for late chronotype" — mock data has `preference: 'late'`, so "How strong is your night owl preference?" should be visible
- Test: "should display shift work checkbox as unchecked" — mock data has `shiftWork: false`, verify the checkbox is unchecked
- Test: "should show past shift work years field when not doing shift work" — since `shiftWork: false`, the "how many years did you do shift work in the past?" field should be visible
- Test: "should display time zone travel checkbox" — assert "I travel across time zones more than 1 time a month" exists
- Test: "should show work/school time field for late preference" — mock data has `preference: 'late'`, so the work/school time field should be visible
- Test: "should display night owl warning" — since preference is 'late', the "Night Owl Chronotype" / "Delayed Sleep Phase Syndrome" warning should be visible

**describe('Sleep Hygiene section'):**
- beforeEach: `cy.navigateToSection('sleep-hygiene')`
- Test: "should display the section title" — `assertSectionVisible(SECTION_TITLES['sleep-hygiene'])`
- Test: "should display supplement checklist" — assert "What supplements or over-the-counter medications do you take for sleep?" label and options like "Melatonin", "Benadryl", "Magnesium" exist
- Test: "should display prescription medication checklist" — assert "What prescription medications do you take for sleep?" label and options like "Benzodiazepines", "Z-drugs", "Orexin blockers" exist
- Test: "should display stimulants and nicotine fields" — assert "Are you prescribed stimulants?" and "I smoke cigarettes or use nicotine patches" labels exist
- Test: "should not show prescription medication warning with mock data" — mock data has empty `prescriptionMeds`, so "Prescription Sleep Medications" warning should NOT be visible

Do NOT use `cy.wait()`. Rely on Cypress retry/timeout behavior.
  </action>
  <verify>Run `pnpm test:e2e --spec cypress/e2e/sections-nightmares-report.cy.ts` — all nightmares, chronotype, and sleep-hygiene tests pass</verify>
  <done>Nightmares: 6 tests verifying dream recall, definitions, nightmare frequency, and conditional alerts. Chronotype: 8 tests verifying preference, strength, shift work, work/school time, and night owl warning. Sleep Hygiene: 5 tests verifying supplements, prescriptions, stimulants, and hidden warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Test bedroom, lifestyle, mental-health, and report sections render correctly</name>
  <files>cypress/e2e/sections-nightmares-report.cy.ts</files>
  <action>
Append to the same test file created in Task 1.

**describe('Bedroom section'):**
- beforeEach: `cy.navigateToSection('bedroom')`
- Test: "should display the section title" — `assertSectionVisible(SECTION_TITLES['bedroom'])`
- Test: "should display all four rating sliders" — assert labels for "How relaxing and comfortable", "How comfortable is your bed", "How dark is your bedroom", "How quiet is your bedroom" exist
- Test: "should display overall bedroom score" — assert "Overall Bedroom Score" heading exists and the score is displayed. Mock data gives (7+8+6+7)/4 = 7.0, assert "7.0" text appears. The message "Good, but room for improvement" (score 6-8 range) should be visible.
- Test: "should not show poor environment warning" — mock average is 7.0 (>= 6), so "Room for Improvement" warning alert should NOT be visible
- Test: "should not show light or noise specific warnings" — mock dark=6 and quiet=7, both >= 4, so "Light Exposure Issue" and "Noise Disturbance" alerts should NOT exist

**describe('Lifestyle section'):**
- beforeEach: `cy.navigateToSection('lifestyle')`
- Test: "should display the section title" — `assertSectionVisible(SECTION_TITLES['lifestyle'])`
- Test: "should display caffeine section with servings and last time" — assert "Caffeine Consumption" heading, servings field, and "What time do you have your final caffeinated food" label exist (mock has caffeinePerDay: 2, >= 1 triggers the time field)
- Test: "should display alcohol section" — assert "Alcohol Consumption" heading and "How many alcoholic drinks" label exist
- Test: "should display exercise section with duration and end time" — assert "Exercise Habits" heading, days per week field, and since mock `exerciseDaysPerWeek: 3` (> 0), "How long do you typically exercise?" and "What time does your exercise typically end?" should be visible
- Test: "should show late caffeine warning" — mock has `lastCaffeineTime: '14:00'` which is >= 14:00 (2 PM), so "Late Caffeine Consumption" warning should be visible
- Test: "should not show high caffeine or high alcohol warnings" — mock has caffeinePerDay: 2 (< 4) and alcoholPerWeek: 3 (< 7), so neither "High Caffeine Intake" nor alcohol warnings should appear

**describe('Mental Health section'):**
- beforeEach: `cy.navigateToSection('mental-health')`
- Test: "should display the section title" — `assertSectionVisible(SECTION_TITLES['mental-health'])`
- Test: "should display worry and anxiety checkboxes" — assert labels for "Worries about the next day", "anxiety or persistent rumination", "I spend time in bed trying to sleep" exist
- Test: "should display cancel activities radio group" — assert "How often do you cancel activities following a night of poor sleep?" and options "Never", "1-2 times a week", "3 or more times a week" exist
- Test: "should display medical conditions checklist" — assert "Medical History" heading and at least some condition labels like "High blood pressure", "Diabetes", "Heart disease" exist
- Test: "should display mental health conditions checklist" — assert "Mental Health History" heading and conditions like "Depression", "Anxiety disorder", "PTSD" exist
- Test: "should show sleep-related anxiety warning with mock data" — mock has `worriesAffectSleep: true` AND `anxietyInBed: true`, so "Sleep-Related Anxiety Detected" should be visible
- Test: "should show significant sleep impact warning" — mock has `cancelsAfterPoorSleep: '1-2week'` which is not 'never', so "Significant Sleep Impact on Daily Life" should be visible
- Test: "should show sleep effort paradox alert" — mock has `timeInBedTrying: true`, so "Sleep Effort Paradox" text should be visible

**describe('Report section'):**
- beforeEach: `cy.navigateToSection('report')`
- Test: "should display the section title" — `assertSectionVisible(SECTION_TITLES['report'])`
- Test: "should display the report content area" — assert that the report section renders without errors. Look for text like "Sleep Report" or "Your Sleep Report" or a report-related heading within the section content. The report section generates diagnosis results based on mock data, so verify it renders without a blank page.
- Test: "should not show Continue button on report section" — `cy.contains('button', 'Continue').should('not.exist')` — report is the last section

Note: The report section is complex (diagnosis algorithms + PDF generation). For this plan, only verify that it renders and shows report content — detailed report text verification is Phase 6.
  </action>
  <verify>Run `pnpm test:e2e --spec cypress/e2e/sections-nightmares-report.cy.ts` — all tests pass including bedroom, lifestyle, mental-health, and report</verify>
  <done>Bedroom: 5 tests verifying sliders, overall score, and hidden warnings. Lifestyle: 6 tests verifying caffeine/alcohol/exercise sections and conditional warnings. Mental Health: 8 tests verifying checkboxes, radio groups, condition checklists, and conditional anxiety/impact/effort alerts. Report: 3 tests verifying section renders and no Continue button. Phase 2 complete — all 16 sections have rendering tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test:e2e --spec cypress/e2e/sections-nightmares-report.cy.ts` passes all tests
- [ ] No TypeScript compilation errors in the test file
- [ ] Existing test files not modified
- [ ] All 7 sections (nightmares, chronotype, sleep-hygiene, bedroom, lifestyle, mental-health, report) have at least 3 tests each
- [ ] Full phase verification: all 3 test files from Phase 2 pass: `pnpm test:e2e --spec cypress/e2e/sections-intro-daytime.cy.ts,cypress/e2e/sections-sleep-parasomnia.cy.ts,cypress/e2e/sections-nightmares-report.cy.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `cypress/e2e/sections-nightmares-report.cy.ts` exists with 7 describe blocks
- All 16 questionnaire sections now have E2E rendering tests across the 3 plan files
- Phase 2 complete — full questionnaire flow coverage achieved
- No modifications to existing test files
</success_criteria>

<output>
After completion, create `.planning/phases/02-full-questionnaire-flow/02-03-SUMMARY.md`
</output>
