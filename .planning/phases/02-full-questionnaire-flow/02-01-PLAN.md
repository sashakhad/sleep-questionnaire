---
phase: 02-full-questionnaire-flow
plan: 01
type: execute
depends_on: []
files_modified: [cypress/e2e/sections-intro-daytime.cy.ts]
---

<objective>
Create Cypress E2E tests verifying that sections intro, demographics, sleep-disorder-diagnoses, and daytime render correctly with pre-filled mock data via the /dev route.

Purpose: Establish the first batch of section-level rendering tests, confirming that each section displays its expected form fields, labels, and UI elements when navigated to with mock data.
Output: `cypress/e2e/sections-intro-daytime.cy.ts` with tests for the first 4 questionnaire sections.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-infrastructure-navigation/01-01-SUMMARY.md
@.planning/phases/01-test-infrastructure-navigation/01-02-SUMMARY.md
@cypress/support/test-data.ts
@cypress/support/commands.ts
@cypress/support/assertions.ts
@src/components/questionnaire/sections/IntroSection.tsx
@src/components/questionnaire/sections/DemographicsSection.tsx
@src/components/questionnaire/sections/SleepDisorderDiagnosesSection.tsx
@src/components/questionnaire/sections/DaytimeSection.tsx
@src/components/questionnaire/QuestionnaireForm.tsx (lines 16-159 for MOCK_DATA)

**Tech stack available:** Cypress 14.x, custom commands (navigateToSection, selectOption, clickRadio, checkCheckbox, uncheckCheckbox)
**Established patterns:** `cy.navigateToSection(slug)` for section navigation, `assertSectionVisible(title)` for card title checks, SECTIONS/SECTION_TITLES/SELECTORS from test-data.ts
**Constraining decisions:**
- Phase 1: Use `/dev` route with mock data for section tests (pre-fills via `MOCK_DATA` constant)
- Phase 1: Import helpers from `../support/test-data` and `../support/assertions`
- Keep existing test files intact — create new files alongside them

**Mock data relevant to these sections (from MOCK_DATA):**
- intro: `{ acceptedDisclaimer: true }`
- demographics: `{ yearOfBirth: 1990, sex: 'male', zipcode: '10001', weight: 165, height: 70 }`
- sleepDisorderDiagnoses: `{ diagnosedDisorders: [], otherDiagnosisDescription: '' }`
- daytime: `{ plannedNaps: { daysPerWeek: 2, duration: '30' }, fallAsleepDuring: ['lectures', 'evening'], sleepinessInterferes: true, sleepinessSeverity: 5, tiredButCantSleep: '3-5days', nonRestorativeSleep: true, tirednessRating: 5, fatigueRating: 3 }`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test intro and demographics sections render correctly</name>
  <files>cypress/e2e/sections-intro-daytime.cy.ts</files>
  <action>
Create `cypress/e2e/sections-intro-daytime.cy.ts`. Import `SECTION_TITLES` from `../support/test-data` and `assertSectionVisible` from `../support/assertions`.

**describe('Intro section'):**
- beforeEach: `cy.navigateToSection('intro')`
- Test: "should display the welcome heading" — assert `h1` contains "Welcome to Your Sleep Assessment"
- Test: "should display the disclaimer checkbox" — assert label text "I have read and understand the service that we provide" exists
- Test: "should show disclaimer checkbox as checked with mock data" — the mock data sets `acceptedDisclaimer: true`, so verify the checkbox button has `aria-checked="true"` or `data-state="checked"`
- Test: "should display all four feature cards" — assert text for "15-20 Minutes", "Nighttime & Daytime", "Free & Anonymous Report", "Expert Guidance" exist
- Test: "should display privacy and before-you-begin alerts" — assert "Your Privacy is Protected" and "Before You Begin" text exist

**describe('Demographics section'):**
- beforeEach: `cy.navigateToSection('demographics')`
- Test: "should display the section title" — `assertSectionVisible(SECTION_TITLES['demographics'])`
- Test: "should display year of birth field" — assert label "What year were you born?" exists
- Test: "should display sex selector" — assert label "Sex" exists
- Test: "should display weight and height fields" — assert labels "Weight (lbs)" and contain height-related label exist
- Test: "should display zipcode field" — assert label "Zipcode" exists
- Test: "should display BMI section with mock data" — mock data has weight 165 and height 70 which gives valid BMI, so assert "Body Mass Index (BMI)" text is visible and the BMI value (approximately 23.7) appears
- Test: "should show age-related alert for mock data birth year" — yearOfBirth 1990 means age ~36 in 2026, which is between 25-55, so no age alert should show. Verify "Age-Related Sleep Changes" does NOT exist

Do NOT use `cy.wait()` for timing — rely on Cypress's built-in retry/timeout. Use `{ timeout: 10000 }` on assertions if needed for hydration.
  </action>
  <verify>Run `pnpm test:e2e --spec cypress/e2e/sections-intro-daytime.cy.ts` — all intro and demographics tests pass</verify>
  <done>Intro section: 5 passing tests verifying welcome heading, disclaimer checkbox state, feature cards, and alerts. Demographics section: 7 passing tests verifying all form fields render and BMI displays with mock data.</done>
</task>

<task type="auto">
  <name>Task 2: Test sleep-disorder-diagnoses and daytime sections render correctly</name>
  <files>cypress/e2e/sections-intro-daytime.cy.ts</files>
  <action>
Append to the same test file created in Task 1.

**describe('Sleep Disorder Diagnoses section'):**
- beforeEach: `cy.navigateToSection('sleep-disorder-diagnoses')`
- Test: "should display the section title" — `assertSectionVisible(SECTION_TITLES['sleep-disorder-diagnoses'])`
- Test: "should display all 11 disorder checkboxes" — assert each label text exists: "Narcolepsy", "Hypersomnia", "Insomnia", "Parasomnia (sleepwalking or sleep terrors)", "Nocturnal enuresis", "Circadian Rhythm Disorder", "Restless Legs Syndrome and/or Periodic Limb Movement Disorder", "Obstructive Sleep Apnea Syndrome", "Central Sleep Apnea Syndrome", "Insufficient Sleep", "REM Behavior Disorder". Count that 11 checkbox items exist within the disorder checklist area.
- Test: "should display other diagnosis text input" — assert label "Other diagnosis (please describe)" and input placeholder "Enter any other diagnoses..." exist
- Test: "should have no disorders checked with mock data" — mock data has `diagnosedDisorders: []`, verify none of the checkboxes have `data-state="checked"`

**describe('Daytime section'):**
- beforeEach: `cy.navigateToSection('daytime')`
- Test: "should display the section title" — `assertSectionVisible(SECTION_TITLES['daytime'])`
- Test: "should display planned naps fields" — assert "Planned Naps" heading and "I take planned naps how many days per week?" label exist. Mock data has daysPerWeek: 2, so nap duration select ("How long are my naps typically?") should also be visible.
- Test: "should display fall asleep during activities checklist" — assert "During a typical week do you fall asleep..." label and all 6 activity options exist ("Stopped at a stop light", "During lectures or work meetings", "While working or studying", "During a conversation", "While engaged in a quiet activity during the evening", "While eating a meal")
- Test: "should show sleepiness interference checkbox as checked" — mock data has `sleepinessInterferes: true`, verify the checkbox for "My sleepiness interferes with my daily activities" is checked
- Test: "should display sleepiness severity slider when interference is checked" — since `sleepinessInterferes` is true in mock data, verify "How severe is the interference?" label and the slider are visible
- Test: "should display tired but can't sleep radio group" — mock data has severity 5 (<=6), so "I feel tired but cannot fall asleep" should be visible with radio options
- Test: "should display sleep quality section" — assert "Sleep Quality" heading, "My sleep does not feel restorative" checkbox (checked per mock data), "Pain affects my ability" checkbox exist
- Test: "should display tiredness and fatigue rating sliders" — assert "Rate Your Daytime Experience" heading, "I rate my tiredness" and "I rate my fatigue" labels exist
  </action>
  <verify>Run `pnpm test:e2e --spec cypress/e2e/sections-intro-daytime.cy.ts` — all tests pass including both new describe blocks</verify>
  <done>Sleep Disorder Diagnoses: 4 passing tests verifying disorder checklist, other input, and empty mock state. Daytime: 8 passing tests verifying planned naps, fall-asleep activities, sleepiness severity, tired-but-can't-sleep, sleep quality, and rating sliders all render with mock data.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test:e2e --spec cypress/e2e/sections-intro-daytime.cy.ts` passes all tests
- [ ] No TypeScript compilation errors in the test file
- [ ] Existing test files (`app.cy.ts`, `round3-feedback.cy.ts`, `navigation.cy.ts`) not modified
- [ ] All 4 sections (intro, demographics, sleep-disorder-diagnoses, daytime) have at least 4 tests each
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `cypress/e2e/sections-intro-daytime.cy.ts` exists with 4 describe blocks
- Each section has tests verifying form field rendering with mock data
- No modifications to existing test files
</success_criteria>

<output>
After completion, create `.planning/phases/02-full-questionnaire-flow/02-01-SUMMARY.md`
</output>
