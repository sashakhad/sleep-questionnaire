---
phase: 04-conditional-logic
plan: 01
type: execute
depends_on: []
files_modified: [cypress/e2e/conditional-chronotype.cy.ts]
---

<objective>
Test conditional field visibility in the Chronotype section: shift work toggles, preference-based fields, and associated warning alerts.

Purpose: Verify that the chronotype section correctly shows/hides fields based on user selections — shift work details appear when shift work is checked, preference strength appears for early/late but not flexible, and warning alerts trigger correctly.
Output: `cypress/e2e/conditional-chronotype.cy.ts` with comprehensive conditional visibility tests.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/questionnaire/sections/ChronotypeSection.tsx
@cypress/support/commands.ts
@cypress/support/test-data.ts
@cypress/support/assertions.ts

**Established patterns:**
- `cy.navigateToSection('chronotype')` navigates via `/dev` route with pre-filled mock data
- Checkbox state assertions: `cy.contains(text).closest('[data-slot="form-item"]').find('button[role="checkbox"]')`
- `cy.checkCheckbox(label)` / `cy.uncheckCheckbox(label)` custom commands for toggling
- `cy.clickRadio(value)` for RadioGroupField selections

**Mock data defaults (chronotype):**
- `preference: 'late'` — preferenceStrength visible, Night Owl warning visible
- `shiftWork: false` — shift details hidden, pastShiftWorkYears visible
- `pastShiftWorkYears: 0` — no shift work impact warning
- `workSchoolTime: '09:00'` — visible (because preference is 'late')

**Constraining decisions:**
- Phase 1: `data-slot="card-title"` added to CardTitle for test selectors
- Phase 1: Use `closest('[data-slot="form-item"]').find('button[role="checkbox"]')` for checkbox state assertions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shift work and preference field visibility tests</name>
  <files>cypress/e2e/conditional-chronotype.cy.ts</files>
  <action>
Create `cypress/e2e/conditional-chronotype.cy.ts` with a top-level `describe('Chronotype Conditional Logic')` block.

**describe('Shift work conditionals'):**
- Test: with default mock data (shiftWork=false), "What type of shift do you work?" and "How many days a week do you work shifts?" should NOT exist in DOM
- Test: with default mock data, pastShiftWorkYears field ("how many years did you do shift work in the past") SHOULD be visible
- Test: check the "My job requires me to do shift work" checkbox → verify "What type of shift do you work?" input and "How many days a week" fields appear
- Test: check shift work checkbox → verify pastShiftWorkYears field disappears (it only shows when shiftWork is false)
- Test: check shift work, then uncheck → verify shift details disappear and pastShiftWorkYears reappears

**describe('Preference conditionals'):**
- Test: with default mock data (preference='late'), preference strength radio ("How strong is your night owl preference?") should be visible
- Test: click 'flexible' radio → verify preference strength field disappears
- Test: click 'early' radio → verify preference strength label changes to "morning preference" and field is visible
- Test: click 'flexible' radio from 'early' → verify preference strength disappears

Use `cy.contains()` to locate fields by their label text. For checking field absence, use `.should('not.exist')`. For checking field presence, use `.should('be.visible')` or `.should('exist')`.

Do NOT use arbitrary `cy.wait()` calls. Rely on Cypress's built-in retry/timeout for element assertions.
  </action>
  <verify>pnpm test:e2e -- --spec cypress/e2e/conditional-chronotype.cy.ts</verify>
  <done>All shift work toggle and preference conditional tests pass — fields appear/disappear correctly based on checkbox and radio state</done>
</task>

<task type="auto">
  <name>Task 2: Chronotype warning alert conditional tests</name>
  <files>cypress/e2e/conditional-chronotype.cy.ts</files>
  <action>
Append to the same test file a new `describe('Chronotype warning alerts')` block.

**Tests:**
- Test: with default mock data (preference='late'), "Night Owl Chronotype" warning alert should be visible
- Test: with default mock data, "Morning Person Chronotype" alert should NOT exist
- Test: click 'early' radio → "Morning Person Chronotype" alert should appear, "Night Owl Chronotype" should disappear
- Test: click 'flexible' radio → neither chronotype warning should exist
- Test: with default mock data (shiftWork=false, pastShiftWorkYears=0), "Shift Work Impact" warning should NOT exist
- Test: check "My job requires me to do shift work" checkbox → "Shift Work Impact" warning should appear
- Test: with shiftWork unchecked, navigate to section, type a non-zero value in pastShiftWorkYears field → "Shift Work Impact" warning should appear (tests pastShiftWorkYears > 0 condition)

Use `cy.contains('strong', 'Night Owl Chronotype')` or `cy.contains('Shift Work Impact')` to locate alerts. Use `.should('exist')` / `.should('not.exist')` for presence checks.

For the pastShiftWorkYears test: the default mock value is 0. Clear the field and type '5' to trigger the warning. Use `cy.contains('label', /how many years/i).closest('[data-slot="form-item"]').find('input')` to locate the field.
  </action>
  <verify>pnpm test:e2e -- --spec cypress/e2e/conditional-chronotype.cy.ts</verify>
  <done>All chronotype warning alert tests pass — Night Owl, Morning Person, and Shift Work Impact alerts appear/disappear based on preference and shift work state</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test:e2e -- --spec cypress/e2e/conditional-chronotype.cy.ts` — all tests pass
- [ ] No other test files broken: `pnpm test:e2e` passes (pre-existing navigation.cy.ts failures acceptable)
- [ ] Test file follows established patterns (navigateToSection, checkbox/radio helpers, no cy.wait)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Shift work checkbox toggles field visibility correctly in both directions
- Preference radio changes correctly show/hide preferenceStrength field
- All 3 chronotype warning alerts (Night Owl, Morning Person, Shift Work Impact) tested for correct conditional display
</success_criteria>

<output>
After completion, create `.planning/phases/04-conditional-logic/04-01-SUMMARY.md`
</output>
