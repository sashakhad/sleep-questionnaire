---
phase: 04-conditional-logic
plan: 03
type: execute
depends_on: []
files_modified: [cypress/e2e/conditional-nightmares-parasomnia.cy.ts]
---

<objective>
Test conditional follow-up field visibility in the Nightmares and Parasomnia sections: dream recall info, bad dream frequency, nightmare frequency and trauma, nightmare disorder warnings, parasomnia follow-up questions, and RBD warning.

Purpose: Verify that follow-up questions and warning alerts in the nightmares and parasomnia sections only appear when their triggering conditions are met, and disappear when those conditions are unmet.
Output: `cypress/e2e/conditional-nightmares-parasomnia.cy.ts` with comprehensive conditional tests for both sections.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/questionnaire/sections/NightmaresSection.tsx
@src/components/questionnaire/sections/ParasomniaSection.tsx
@cypress/support/commands.ts
@cypress/support/test-data.ts

**Established patterns:**
- `cy.navigateToSection('nightmares')` / `cy.navigateToSection('parasomnia')` for section navigation
- `cy.checkCheckbox(label)` / `cy.uncheckCheckbox(label)` for toggling
- Checkbox state: `closest('[data-slot="form-item"]').find('button[role="checkbox"]')`
- NumberField modification: locate via label, find input, `.clear().type(value)`

**Mock data defaults (nightmares):**
- `remembersDreams: true` → dream recall info alert visible
- `hasBadDreams: false` → badDreamsPerWeek hidden
- `hasNightmares: true` → nightmaresPerWeek (1) + associatedWithTrauma visible
- `nightmaresPerWeek: 1` → "less frequent nightmares info" visible (>0 and <2), "Possible Nightmare Disorder" NOT visible (<2)
- `associatedWithTrauma: false` → Trauma-Related Nightmares alert hidden

**Mock data defaults (parasomnia):**
- `nightBehaviors: []` → follow-up questions (remembersEvents, actsOutDreams, hasInjuredOrLeftHome) hidden
- `remembersEvents: false`, `actsOutDreams: false` → RBD Warning hidden

**Conditionals being tested (nightmares):**
- `remembersDreams` → dream recall info alert
- `hasBadDreams` → badDreamsPerWeek NumberField
- `badDreamsPerWeek >= 3` → "Frequent Bad Dreams" warning
- `hasNightmares` → nightmaresPerWeek + associatedWithTrauma fields
- `nightmaresPerWeek >= 2` → "Possible Nightmare Disorder" warning
- `hasNightmares && nightmaresPerWeek > 0 && nightmaresPerWeek < 2` → less frequent nightmares info
- `associatedWithTrauma` → "Trauma-Related Nightmares" alert

**Conditionals being tested (parasomnia):**
- `nightBehaviors.length > 0` → remembersEvents, actsOutDreams, hasInjuredOrLeftHome checkboxes + "Safety Measures" warning
- `remembersEvents && actsOutDreams` → "REM Behavioral Disorder" alert
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nightmare section conditional follow-ups</name>
  <files>cypress/e2e/conditional-nightmares-parasomnia.cy.ts</files>
  <action>
Create `cypress/e2e/conditional-nightmares-parasomnia.cy.ts` with a top-level `describe('Nightmare and Parasomnia Conditionals')`.

**describe('Nightmare conditionals'):**
Each test uses `cy.navigateToSection('nightmares')`.

Dream recall:
- Test: with mock data (remembersDreams=true), dream recall info alert containing "Remembering your dreams can be a function" should exist
- Test: uncheck "I remember my dreams" → info alert should disappear

Bad dreams:
- Test: with mock data (hasBadDreams=false), "How many nights a week do you have bad dreams?" should NOT exist
- Test: check "I have bad dreams, but not nightmares" → badDreamsPerWeek field should appear
- Test: check bad dreams, type '3' in frequency → "Frequent Bad Dreams" warning should appear
- Test: check bad dreams, type '2' in frequency → "Frequent Bad Dreams" warning should NOT exist (threshold is >= 3)

Nightmares:
- Test: with mock data (hasNightmares=true, nightmaresPerWeek=1), "How many nights a week do you have nightmares?" field should be visible
- Test: with mock data, "associated with exposure to trauma" checkbox should be visible
- Test: with mock data (nightmaresPerWeek=1), less frequent nightmares info alert ("Nightmares can disturb sleep") should exist
- Test: with mock data (nightmaresPerWeek=1), "Possible Nightmare Disorder" warning should NOT exist
- Test: clear nightmaresPerWeek and type '2' → "Possible Nightmare Disorder" warning should appear, less frequent info should disappear
- Test: uncheck "I have nightmares" → nightmaresPerWeek field, associatedWithTrauma, and less frequent info should all disappear

Trauma:
- Test: with mock data (associatedWithTrauma=false), "Trauma-Related Nightmares" alert should NOT exist
- Test: check "associated with exposure to trauma" → "Trauma-Related Nightmares" alert should appear

Locate nightmare frequency field by label text "How many nights a week do you have nightmares?". Locate bad dream frequency by "How many nights a week do you have bad dreams?". Use `cy.contains('strong', 'text')` or `cy.contains('text')` for alert detection.
  </action>
  <verify>pnpm test:e2e -- --spec cypress/e2e/conditional-nightmares-parasomnia.cy.ts</verify>
  <done>All nightmare conditional tests pass — dream recall info, bad dream frequency, nightmare frequency, all warning alerts appear/disappear at correct thresholds</done>
</task>

<task type="auto">
  <name>Task 2: Parasomnia section conditional follow-ups</name>
  <files>cypress/e2e/conditional-nightmares-parasomnia.cy.ts</files>
  <action>
Append to the same test file a new `describe('Parasomnia conditionals')` block. Each test uses `cy.navigateToSection('parasomnia')`.

Night behaviors follow-ups:
- Test: with mock data (nightBehaviors=[]), "I have a clear memory of these events" checkbox should NOT exist, "I act out my dreams" should NOT exist, "I have injured myself" should NOT exist
- Test: with mock data, "Safety Measures Recommended" warning should NOT exist
- Test: check one of the night behavior checkboxes (e.g., "Walk") → "I have a clear memory of these events", "I act out my dreams", "I have injured myself or others" checkboxes should all appear
- Test: check a night behavior → "Safety Measures Recommended" warning should appear
- Test: check a night behavior, then uncheck it → follow-up checkboxes and safety warning should disappear

To check a night behavior checkbox: the checkboxes in the nightBehaviors FormField use `Checkbox` inside `FormItem` with a `FormLabel`. Find by text "Walk" — use `cy.contains('Walk').closest('.flex').find('[role="checkbox"]').click({ force: true })` or similar approach matching the existing checkbox list pattern.

RBD Warning:
- Test: check a night behavior (to show follow-ups), then check both "I have a clear memory of these events" AND "I act out my dreams" → "REM Behavioral Disorder" alert with text "Important Safety Notice" should appear
- Test: check a night behavior, check remembersEvents only (not actsOutDreams) → RBD alert should NOT exist
- Test: check a night behavior, check actsOutDreams only (not remembersEvents) → RBD alert should NOT exist (both must be true)
  </action>
  <verify>pnpm test:e2e -- --spec cypress/e2e/conditional-nightmares-parasomnia.cy.ts</verify>
  <done>All parasomnia conditional tests pass — night behavior follow-ups appear when behaviors selected, RBD warning requires both remembersEvents AND actsOutDreams</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test:e2e -- --spec cypress/e2e/conditional-nightmares-parasomnia.cy.ts` — all tests pass
- [ ] No other test files broken: `pnpm test:e2e` passes (pre-existing navigation.cy.ts failures acceptable)
- [ ] Test file follows established patterns (navigateToSection, checkbox helpers, no cy.wait)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Nightmare section: all 7 conditional branches tested (dream recall, bad dreams, bad dream frequency warning, nightmare follow-ups, nightmare disorder warning, less frequent info, trauma alert)
- Parasomnia section: nightBehaviors toggle correctly shows/hides 3 follow-up checkboxes and safety warning
- RBD warning correctly requires BOTH remembersEvents AND actsOutDreams
</success_criteria>

<output>
After completion, create `.planning/phases/04-conditional-logic/04-03-SUMMARY.md`
</output>
