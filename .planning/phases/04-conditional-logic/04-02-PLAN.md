---
phase: 04-conditional-logic
plan: 02
type: execute
depends_on: []
files_modified: [cypress/e2e/conditional-lifestyle.cy.ts]
---

<objective>
Test conditional field visibility for the work/school time field in the chronotype section (hides when "flexible" is chosen) and the lifestyle section conditionals (caffeine time, exercise details, and warning alerts).

Purpose: Verify that the work/school time field correctly appears/hides based on preference and shift work state, and that lifestyle fields (lastCaffeineTime, exerciseDuration, exerciseEndTime) appear only when their parent conditions are met.
Output: `cypress/e2e/conditional-lifestyle.cy.ts` with work/school time and lifestyle conditional visibility tests.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/questionnaire/sections/ChronotypeSection.tsx
@src/components/questionnaire/sections/LifestyleSection.tsx
@cypress/support/commands.ts
@cypress/support/test-data.ts

**Established patterns:**
- `cy.navigateToSection('chronotype')` / `cy.navigateToSection('lifestyle')` for section navigation
- `cy.checkCheckbox(label)` / `cy.uncheckCheckbox(label)` for toggling checkboxes
- `cy.clickRadio(value)` for RadioGroupField selections
- Checkbox state: `closest('[data-slot="form-item"]').find('button[role="checkbox"]')`

**Mock data defaults:**
- chronotype: `preference: 'late'`, `shiftWork: false` → workSchoolTime IS visible (late || shiftWork condition met)
- lifestyle: `caffeinePerDay: 2` → lastCaffeineTime visible; `exerciseDaysPerWeek: 3` → exerciseDuration + exerciseEndTime visible

**Conditionals being tested:**
- ChronotypeSection: `(preference === 'early' || preference === 'late' || shiftWork)` → workSchoolTime
- LifestyleSection: `caffeinePerDay >= 1` → lastCaffeineTime TimeField
- LifestyleSection: `exerciseDaysPerWeek > 0` → exerciseDuration + exerciseEndTime
- LifestyleSection: `caffeinePerDay > 4` → High Caffeine warning
- LifestyleSection: `exerciseDaysPerWeek === 0` → No Regular Exercise warning
- LifestyleSection: `exerciseDaysPerWeek >= 5` → Great Exercise Habits alert
</context>

<tasks>

<task type="auto">
  <name>Task 1: Work/school time conditional visibility tests</name>
  <files>cypress/e2e/conditional-lifestyle.cy.ts</files>
  <action>
Create `cypress/e2e/conditional-lifestyle.cy.ts` with a top-level `describe('Work/School Time and Lifestyle Conditionals')`.

**describe('Work/school time visibility'):**
Each test navigates to the chronotype section with `cy.navigateToSection('chronotype')`.

- Test: with default mock data (preference='late', shiftWork=false), the work/school time field ("what time do you have to be at work/school") should be visible
- Test: click 'flexible' radio → work/school time field should NOT exist (neither early/late preference nor shiftWork is true)
- Test: click 'flexible' radio, then check "My job requires me to do shift work" checkbox → work/school time field should reappear (shiftWork condition now true, even though preference is flexible)
- Test: with default (preference='late'), click 'early' radio → work/school time field should remain visible (early preference satisfies condition)

Locate the work/school time field by searching for label text containing "what time do you have to be at work/school" (case-insensitive). Use `.should('exist')` / `.should('not.exist')`.
  </action>
  <verify>pnpm test:e2e -- --spec cypress/e2e/conditional-lifestyle.cy.ts</verify>
  <done>Work/school time field correctly shows for early/late/shiftWork and hides for flexible+no-shiftWork</done>
</task>

<task type="auto">
  <name>Task 2: Lifestyle section field visibility and warning conditionals</name>
  <files>cypress/e2e/conditional-lifestyle.cy.ts</files>
  <action>
Append to the same test file two new describe blocks for the lifestyle section.

**describe('Caffeine conditionals'):**
Each test navigates to `cy.navigateToSection('lifestyle')`.

- Test: with default mock data (caffeinePerDay=2), the "What time do you have your final caffeinated food or beverage?" TimeField should be visible
- Test: clear caffeinePerDay input and type '0' → lastCaffeineTime field should disappear
- Test: with caffeinePerDay=0, type '1' → lastCaffeineTime field should reappear
- Test: with default mock data (caffeinePerDay=2), "High Caffeine Intake" warning should NOT exist. Clear input and type '5' → "High Caffeine Intake" warning should appear

To modify the caffeinePerDay NumberField: locate via `cy.contains('label', /how many servings/i).closest('[data-slot="form-item"]').find('input')`, then `.clear().type('0')`.

**describe('Exercise conditionals'):**
Each test navigates to `cy.navigateToSection('lifestyle')`.

- Test: with default mock data (exerciseDaysPerWeek=3), "How long do you typically exercise?" and "What time does your exercise typically end?" fields should be visible
- Test: clear exerciseDaysPerWeek input and type '0' → exercise duration and end time fields should disappear, and "No Regular Exercise" warning should appear
- Test: clear and type '5' → exercise fields visible, "Great Exercise Habits" alert should appear
- Test: clear and type '0', then type '2' → exercise fields reappear, no exercise warning disappears

Locate exerciseDaysPerWeek via `cy.contains('label', /how many days do you exercise/i).closest('[data-slot="form-item"]').find('input')`.
  </action>
  <verify>pnpm test:e2e -- --spec cypress/e2e/conditional-lifestyle.cy.ts</verify>
  <done>All caffeine and exercise conditional field visibility and warning alert tests pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test:e2e -- --spec cypress/e2e/conditional-lifestyle.cy.ts` — all tests pass
- [ ] No other test files broken: `pnpm test:e2e` passes (pre-existing navigation.cy.ts failures acceptable)
- [ ] Test file follows established patterns (navigateToSection, checkbox/radio helpers, no cy.wait)
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Work/school time field correctly hides when preference is "flexible" AND shiftWork is false
- Work/school time field correctly appears when any of early/late/shiftWork is true
- Caffeine time field appears/disappears based on caffeinePerDay threshold
- Exercise fields appear/disappear based on exerciseDaysPerWeek threshold
- Warning alerts (High Caffeine, No Exercise, Great Exercise) trigger at correct thresholds
</success_criteria>

<output>
After completion, create `.planning/phases/04-conditional-logic/04-02-SUMMARY.md`
</output>
