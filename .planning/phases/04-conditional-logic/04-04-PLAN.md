---
phase: 04-conditional-logic
plan: 04
type: execute
depends_on: []
files_modified: [cypress/e2e/conditional-daytime-restless.cy.ts]
---

<objective>
Test conditional field visibility in the Daytime and Restless Legs sections: pain severity slider, leg cramp frequency, planned nap duration, sleepiness severity chain (interference → severity → safety warning / tired-but-can't-sleep), and narcolepsy/cataplexy question chain.

Purpose: Verify that the daytime section's deeply nested conditional chains (sleepiness → severity → subsequent fields) and the restless legs section's leg cramp conditionals work correctly.
Output: `cypress/e2e/conditional-daytime-restless.cy.ts` with comprehensive conditional tests for both sections.
</objective>

<execution_context>
~/.cursor/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/questionnaire/sections/DaytimeSection.tsx
@src/components/questionnaire/sections/RestlessLegsSection.tsx
@cypress/support/commands.ts
@cypress/support/test-data.ts

**Established patterns:**
- `cy.navigateToSection('daytime')` / `cy.navigateToSection('restless-legs')` for section navigation
- `cy.checkCheckbox(label)` / `cy.uncheckCheckbox(label)` for toggling
- Checkbox state: `closest('[data-slot="form-item"]').find('button[role="checkbox"]')`
- NumberField modification: locate via label, find input, `.clear().type(value)`
- Slider interaction: `cy.get('.slider-selector').invoke('val', N).trigger('input')` or use Cypress `trigger('pointerdown')` approach

**Mock data defaults (daytime):**
- `plannedNaps.daysPerWeek: 2` → nap duration select visible
- `fallAsleepDuring: ['lectures', 'evening']` → 2 activities checked
- `sleepinessInterferes: true` → severity slider visible (severity=5)
- `sleepinessSeverity: 5` → safety warning NOT visible (<=8), tiredButCantSleep visible (<=6)
- `painAffectsSleep: false` → painSeverity hidden
- `weaknessWhenExcited: []` → narcolepsy/cataplexy alert NOT visible (no cataplexy, dozing score=2, not >6)

**Mock data defaults (restlessLegs):**
- `legCramps: false` → legCrampsPerWeek hidden
- `troubleLyingStill: false`, `urgeToMoveLegs: false`, `movementRelieves: false` → RLS warning hidden

**Conditionals being tested (daytime):**
- `plannedNaps.daysPerWeek > 0` → nap duration SelectField
- `sleepinessInterferes` → sleepinessSeverity slider
- `sleepinessSeverity > 8` → "Important Safety Warning"
- `sleepinessInterferes && sleepinessSeverity <= 6` → tiredButCantSleep radio
- `fallAsleepDuring.length > 0 && sleepinessInterferes` → weakness when excited (narcolepsy questions)
- `painAffectsSleep` → painSeverity slider
- `nonRestorativeSleep && jointMusclePain && sleepinessInterferes` → Chronic Fatigue warning

**Conditionals being tested (restlessLegs):**
- `legCramps` → legCrampsPerWeek NumberField
- `hasSymptoms` (troubleLyingStill || urgeToMoveLegs || movementRelieves) → RLS warning
- `legCrampsPerWeek >= 2` → "Frequent Nocturnal Leg Cramps" warning
</context>

<tasks>

<task type="auto">
  <name>Task 1: Daytime section conditional chains</name>
  <files>cypress/e2e/conditional-daytime-restless.cy.ts</files>
  <action>
Create `cypress/e2e/conditional-daytime-restless.cy.ts` with a top-level `describe('Daytime and Restless Legs Conditionals')`.

**describe('Planned nap conditionals'):**
Each test uses `cy.navigateToSection('daytime')`.

- Test: with mock data (daysPerWeek=2), "How long are my naps typically?" select should be visible
- Test: clear daysPerWeek input and type '0' → nap duration select should disappear

Locate daysPerWeek via `cy.contains('label', /planned naps.*days per week/i).closest('[data-slot="form-item"]').find('input')` or by searching for "I take planned naps how many days per week". Locate nap duration by label "How long are my naps typically?".

**describe('Sleepiness severity chain'):**
Each test uses `cy.navigateToSection('daytime')`.

- Test: with mock data (sleepinessInterferes=true, severity=5), severity slider section ("How severe is the interference?") should be visible
- Test: with mock data (severity=5), "I feel tired but cannot fall asleep" radio group should be visible (severity <= 6)
- Test: with mock data (severity=5), "Important Safety Warning" should NOT exist (severity <= 8)
- Test: uncheck "My sleepiness interferes with my daily activities" → severity slider and tiredButCantSleep should both disappear
- Test: with sleepinessInterferes checked, the narcolepsy questions ("When I laugh or feel excited") should be visible (because fallAsleepDuring has items AND sleepinessInterferes is true)
- Test: uncheck sleepinessInterferes → narcolepsy questions should disappear (requires both conditions)

For the severity slider tests that check threshold-dependent visibility (severity > 8 → safety warning, severity <= 6 → tiredButCantSleep), the mock defaults to 5 which satisfies both conditions nicely. Do NOT attempt to programmatically change the slider value — testing the default state at severity=5 is sufficient to verify both branches.

**describe('Pain severity conditional'):**
- Test: with mock data (painAffectsSleep=false), "How severe is my pain" slider should NOT exist
- Test: check "Pain affects my ability to fall asleep or stay asleep" → painSeverity slider should appear
- Test: check painAffectsSleep, then uncheck → painSeverity slider should disappear

**describe('Chronic Fatigue warning'):**
- Test: with mock data (nonRestorativeSleep=true, jointMusclePain=false, sleepinessInterferes=true), "Potential Chronic Fatigue Symptoms" warning should NOT exist (jointMusclePain is false)
- Test: check "I experience joint and muscle pain during the day" → "Potential Chronic Fatigue Symptoms" warning should appear (all 3 conditions now true)
  </action>
  <verify>pnpm test:e2e -- --spec cypress/e2e/conditional-daytime-restless.cy.ts</verify>
  <done>All daytime conditional chain tests pass — nap duration, severity slider, tiredButCantSleep, narcolepsy questions, pain severity, and chronic fatigue warning all toggle correctly</done>
</task>

<task type="auto">
  <name>Task 2: Restless legs section conditionals</name>
  <files>cypress/e2e/conditional-daytime-restless.cy.ts</files>
  <action>
Append to the same test file a new `describe('Restless legs conditionals')` block. Each test uses `cy.navigateToSection('restless-legs')`.

**Leg cramp frequency:**
- Test: with mock data (legCramps=false), "How many nights per week do you experience leg cramps?" should NOT exist
- Test: check "I experience leg cramps at night" → legCrampsPerWeek field should appear
- Test: check legCramps, type '2' in frequency → "Frequent Nocturnal Leg Cramps" warning should appear
- Test: check legCramps, type '1' in frequency → "Frequent Nocturnal Leg Cramps" warning should NOT exist (threshold is >= 2)
- Test: check legCramps, then uncheck → legCrampsPerWeek field should disappear

**RLS symptom warning:**
- Test: with mock data (all symptoms false), RLS warning ("Your answers suggest that you may have restless legs syndrome") should NOT exist
- Test: check "I have trouble lying still while trying to fall asleep at night" → RLS warning should appear
- Test: check "I have an urge to move my legs" → RLS warning should appear
- Test: check "Movement relieves the uncomfortable feelings" → RLS warning should appear
- Test: check one symptom then uncheck it → warning should disappear (if it was the only checked symptom)

Locate legCrampsPerWeek by label "How many nights per week do you experience leg cramps?". Locate the field input inside `[data-slot="form-item"]`.
  </action>
  <verify>pnpm test:e2e -- --spec cypress/e2e/conditional-daytime-restless.cy.ts</verify>
  <done>All restless legs conditional tests pass — leg cramp frequency appears on check, frequency warning at >= 2, RLS warning appears when any core symptom checked</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm test:e2e -- --spec cypress/e2e/conditional-daytime-restless.cy.ts` — all tests pass
- [ ] No other test files broken: `pnpm test:e2e` passes (pre-existing navigation.cy.ts failures acceptable)
- [ ] Test file follows established patterns (navigateToSection, checkbox helpers, no cy.wait)
- [ ] Phase 4 complete: all 4 plans executed, all conditional logic tested
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Planned nap duration correctly toggles on daysPerWeek threshold
- Sleepiness severity chain: interference → severity → safety warning / tiredButCantSleep tested
- Narcolepsy questions correctly require both fallAsleepDuring items AND sleepinessInterferes
- Pain severity slider appears/disappears on painAffectsSleep toggle
- Chronic Fatigue warning requires all 3 conditions
- Leg cramp frequency appears on legCramps check, warning at >= 2 threshold
- RLS warning appears when any of the 3 core symptoms is checked
- Phase 4 complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-conditional-logic/04-04-SUMMARY.md`
</output>
