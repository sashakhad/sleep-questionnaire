name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
        with:
          version: 10.8.1
      - uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'pnpm'
      - name: Install dependencies
        run: pnpm install --frozen-lockfile || pnpm install
      - name: Generate Prisma Client
        run: pnpm run db:generate
      - name: Lint
        run: pnpm run lint
      - name: Type Check
        run: pnpm exec tsc --noEmit
      - name: Test
        run: pnpm test

  e2e:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
        with:
          version: 10.8.1
      - uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'pnpm'
      - name: Install dependencies
        run: pnpm install --frozen-lockfile || pnpm install
      - name: Generate Prisma Client
        run: pnpm run db:generate
      - name: Build app
        run: pnpm run build
      - name: Install Cypress binary
        run: pnpm exec cypress install
      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          start: pnpm start
          wait-on: 'http://localhost:3000'
          wait-on-timeout: 60
          spec: cypress/e2e/!(admin-dashboard|api-endpoints).cy.ts
